# ---------------------------------------------------------------------------
# The COLLECTOR (Part A, the `kinea` package) uses ONLY the Python standard
# library — nothing below is needed to fetch or store data. It is listed here
# purely for the Streamlit DASHBOARD (Part B) and the test suite.
# ---------------------------------------------------------------------------

# Dashboard (Part B)
streamlit==1.59.2
altair==6.2.2
pandas==3.0.3
pyarrow==24.0.0
matplotlib==3.10.8

# Tests
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.15.22
mypy==2.3.0
pandas-stubs==3.0.3.260530
hypothesis==6.156.9

# Optional: only for scripts/capture_dashboard_screenshots.py (real dashboard screenshots)
playwright==1.61.0
Pillow==12.3.0
